## 目标
- 让 VegetationProfile 列表与预制体预览列表在新增/删除/赋值后即时刷新且无闪烁
- 在 AssetPreview 返回为空时进行异步重试，确保缩略图能补齐
- 保持不新增文件、仅在现有类中增加轻量辅助方法

## 具体改造
### 1) 预制体预览列表（PreviewGridView）
- 在 bindItem 中当 tex 为空时，使用 EditorApplication.delayCall 或 root.schedule 延迟重试：再次获取 AssetPreview，如果仍为空则使用 mini thumbnail，最后调用 ListView.RefreshItems 并 UpdateSelectionVisuals。
- 抽取私有方法：TryUpdatePreview(Image img, VegetationItem it)；在 delay 重试后统一刷新。
- 渲染末尾：保持现有 Rebuild（仅在数量或分页变化时）+ 始终 RefreshItems；最后 schedule UpdateSelectionVisuals（已有）。

### 2) VegetationProfile 列表（Control.cs）
- RefreshVegetationListUI：设置 itemsSource 后始终调用 ListView.RefreshItems；仅当数量变化时 Rebuild。
- 在新增/删除 Profile 后，统一调用 ReloadAvailableProfiles、RefreshVegetationListUI、UpdatePropertyPanelFromSelectedItem。

### 3) 选择状态与回调
- ThumbListView 左/右键交互保持当前实现；右键批量设置类型后，调度 RefreshPreviewListUI 与轻量样式更新（已有）。
- PrefabAssignmentController 中新增/删除后确保 EditorUtility.SetDirty(profile) 并调用窗口的 RefreshPreviewListUI/RefreshVegetationListUI（已大部分实现，补齐漏点）。

### 4) 按 name 的 SetClickHandler 统一
- 已完成 Start/Settings 中的按钮绑定替换；检查其余页面的按钮（若有）按相同方式替换。

## 验证
- 在预制体列表中新增/删除/空槽赋值：缩略图与类型标签即时刷新
- 快速切换分页与选择：仅在数量或分页变化时 Rebuild；其他情况下 RefreshItems 即可
- 大量新增/删除后列表不闪烁，窗口与 Overlay 重绘稳定（配合现有 16ms 限流）

## 变更范围（不新增文件）
- Editor/Views/PreviewGridView.cs：新增 TryUpdatePreview、延迟重试与 RefreshItems 调度
- Editor/MrTerrainPainterWindow.Control.cs：RefreshVegetationListUI 始终 RefreshItems；数量变化才 Rebuild
- Editor/Controllers/PrefabAssignmentController.cs：新增/删除后统一调用窗口刷新（若有漏点则补齐）

确认后我将按上述步骤提交改动并在编辑器内逐项验证。